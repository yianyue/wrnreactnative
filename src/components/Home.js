'use strict';

import React from 'react-native';
const { PropTypes, View, Text } = React;

class Home extends React.Component {
  static propTypes = {

  };

  render () {
    return (
      <View>
        <Text>
          Home view!
        </Text>
      </View>
    );
  }
}

export default Home;
